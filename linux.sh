# shared with linux server and mac
alias less='less -R' # respect ANSI color escape sequences (i.e. correctly show colorized output)
alias rb='. ~/.zshrc'
alias ls='ls -lG' # colors + always use the long format
